import {MemeType} from "src/types";

type MemeStatProps = {
    meme: MemeType
}

export const MemeStat = ({meme}: MemeStatProps) => {
    return (
        <div style={{height: 500}}>
        {/*TODO: add meme stats*/}
        </div>
    )
}