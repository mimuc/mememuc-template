import {Meme} from "src/types";

type MemeStatProps = {
    meme: Meme
}

export const MemeStat = ({meme}: MemeStatProps) => {
    return (
        <div style={{height: 500}}>
        {/*TODO*/}
        </div>
    )
}